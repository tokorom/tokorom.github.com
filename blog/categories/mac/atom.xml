<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: mac | TOKOROM BLOG]]></title>
  <link href="http://tokorom.github.com/blog/categories/mac/atom.xml" rel="self"/>
  <link href="http://tokorom.github.com/"/>
  <updated>2012-07-15T03:41:58+09:00</updated>
  <id>http://tokorom.github.com/</id>
  <author>
    <name><![CDATA[Yuta ToKoRo]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Macにpandocをインストール中にhs-httpで引っかかった]]></title>
    <link href="http://tokorom.github.com/blog/2012/07/15/install-pandoc/"/>
    <updated>2012-07-15T03:13:00+09:00</updated>
    <id>http://tokorom.github.com/blog/2012/07/15/install-pandoc</id>
    <content type="html"><![CDATA[<h2>hs-httpがインストールできない!</h2>

<p>Macにpandocを入れようと気軽にMacPortsで</p>

<p><code>sh
$ sudo port install pandoc
</code></p>

<p>したところ <em>hs-http</em> のインストールのところでなにやらエラー...</p>

<p>試しに <em>hs-http</em> だけインストールしてみたところ、</p>

<p>``` sh
$ sudo port -d install hs-http
  ...
  Configuring HTTP-4000.1.1...
  ghc: could not execute: /usr/bin/gcc-4.2
  Command failed:  cd "/opt/local/var/macports/build/_opt_local_var_macports_sources_rsync.macports.org_release_ports_devel_hs-HTTP/hs-http/work/HTTP-4000.1.1" &amp;&amp; runhaskell Setup co
  nfigure --prefix=/opt/local --with-compiler=/opt/local/bin/ghc
  Exit code: 1
  Error: org.macports.configure for port hs-http returned: configure failure: command execution failed
  DEBUG: Error code: NONE
  DEBUG: Backtrace: configure failure: command execution failed</p>

<pre><code>  while executing
</code></pre>

<p>  "$procedure $targetname"
  Warning: targets not executed for hs-http: org.macports.activate org.macports.configure org.macports.build org.macports.destroot org.macports.install
  Please see the log file for port hs-http for details:</p>

<pre><code>  /opt/local/var/macports/logs/_opt_local_var_macports_sources_rsync.macports.org_release_ports_devel_hs-HTTP/hs-http/main.log
</code></pre>

<p>  To report a bug, follow the instructions in the guide:    http://guide.macports.org/#project.tickets
  Error: Processing of port hs-http failed
```</p>

<p>と、<em>gcc-4.2</em> が実行できないというエラーが発生している。</p>

<h2>gcc-4.2 を用意する</h2>

<p>それではと gcc-4.2 が入っているか見てみたところ、</p>

<p><code>sh
$ ll /usr/bin/gcc*
  lrwxr-xr-x  1 root  wheel  12 12  4  2011 /usr/bin/gcc -&gt; llvm-gcc-4.2
</code></p>

<p>と <em>llvm-gcc-4.2</em> はあるものの <em>gcc-4.2</em> はない模様。<br/>
それでは <em>gcc-4.2</em> を入れましょうと、</p>

<p><code>sh
$ sudo port install gcc42
</code></p>

<p>としてみたものの、Snow Leopard以降にはインストールできないという冷たいお返事。
それならそれで、 <em>gcc-4.2</em> を <em>llvm-gcc-4.2</em> にリンクして使っちゃうことにします。</p>

<p><code>sh
$ sudo ln -s /usr/bin/llvm-gcc-4.2 /usr/bin/gcc-4.2
</code></p>

<h2>pandocのインストール</h2>

<p>これで晴れて <em>gcc-4.2</em> が存在することになりましたので、</p>

<p><code>sh
$ sudo port -d install hs-http
</code></p>

<p>がばっちり成功しました!
もちろんそもそものpandocも、</p>

<p><code>sh
$ sudo port install pandoc
</code></p>

<p>で普通にインストールできましたとさ。</p>
]]></content>
  </entry>
  
</feed>
